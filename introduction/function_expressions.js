/* Function Expressions
*/

// function statements
function someFunction(arg) {
  // code to perform an action but does not produce an immediate product
  if (x === 5) {
    // do something
  }
}

// function expressions
var someFunc = function(arg) {
  // produces an output
  // expression examples
  3 + 4;
  var x = 3;
}
